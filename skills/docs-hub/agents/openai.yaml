interface:
  display_name: "docs-hub"
  short_description: "资料查询默认先走本地 DocsHub，支持初始化与刷新"
  default_prompt: "Use $docs-hub as the mandatory first step for documentation and factual lookups; query the local DocsHub before answering or falling back to memory/web unless the hub is unavailable, yields no useful result after refinement, or the user explicitly requests online-latest information."

policy:
  allow_implicit_invocation: true
