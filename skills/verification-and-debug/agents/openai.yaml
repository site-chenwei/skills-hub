interface:
  display_name: "verification-and-debug"
  short_description: "Capture failures, isolate the broken layer, and verify root-cause fixes."
  default_prompt: "Use $verification-and-debug to capture the failing command, classify the broken layer, and verify the smallest root-cause fix. When invoking bundled helpers, derive <skill_root> from the opened SKILL.md path and call python3 <skill_root>/run.py ..."
