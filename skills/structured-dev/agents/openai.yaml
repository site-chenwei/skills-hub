interface:
  display_name: "structured-dev"
  short_description: "Generate change briefs, choose workflow depth, and enforce validation gates."
  default_prompt: "Use $structured-dev to generate a change brief, choose light vs full workflow, and deliver the task with explicit review and verification gates. When invoking bundled helpers, derive <skill_root> from the opened SKILL.md path and call python3 <skill_root>/run.py ..."
